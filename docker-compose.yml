version: '3.8'

services:
  inventory-app:
    build: .
    container_name: dynamic-inventory-app
    ports:
      - "9090:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
